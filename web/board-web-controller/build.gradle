bootJar {
    enabled = true
}

bootRun {
    enabled = true
}

dependencies {
    implementation(project(":web:board-web-service"))
    implementation("org.springframework.boot:spring-boot-starter-web")
}